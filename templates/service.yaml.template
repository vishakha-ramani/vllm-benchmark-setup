kind: Service
apiVersion: v1
metadata:
  name: {name}
  namespace: {namespace}
  labels:
    app: {app_label}
    experiment: vllm-sweep
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
  selector:
    app: {app_label}
  type: ClusterIP
